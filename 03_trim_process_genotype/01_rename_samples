# rename the samples for compatability with the cluster array jobs

cd /Volumes/Genomic2017/05_zosterops_genomes/01_raw_fastq
while read -r name1 name2; do
	mv $name1 $name2
done < rename_genomes_a.txt
